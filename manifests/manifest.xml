<?xml version="1.0" encoding="UTF-8"?>
<OfficeApp
  xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
  xmlns:mailappor="http://schemas.microsoft.com/office/mailappversionoverrides/1.0"
  xsi:type="MailApp">

  <!-- Basic Add-in Identity -->
  <Id>{unique-guid}</Id>
  <Version>1.0.0.0</Version>
  <ProviderName>RIB Software GmbH</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>
  <DisplayName DefaultValue="Quill - AI Mail Assistant"/>
  <Description DefaultValue="AI-powered email assistant with multi-provider LLM support. Summarize emails, draft replies, extract data, and translate — all within Outlook."/>
  <IconUrl DefaultValue="https://{domain}/assets/icon-64.png"/>
  <HighResolutionIconUrl DefaultValue="https://{domain}/assets/icon-128.png"/>
  <SupportUrl DefaultValue="https://{domain}/support"/>

  <AppDomains>
    <AppDomain>https://{domain}</AppDomain>
  </AppDomains>

  <!-- Outlook Host -->
  <Hosts>
    <Host Name="Mailbox"/>
  </Hosts>

  <!-- Minimum Requirement Set -->
  <Requirements>
    <Sets>
      <Set Name="Mailbox" MinVersion="1.5"/>
    </Sets>
  </Requirements>

  <!-- Form Settings for Legacy Clients -->
  <FormSettings>
    <Form xsi:type="ItemRead">
      <DesktopSettings>
        <SourceLocation DefaultValue="https://{domain}/taskpane.html"/>
        <RequestedHeight>450</RequestedHeight>
      </DesktopSettings>
    </Form>
    <Form xsi:type="ItemEdit">
      <DesktopSettings>
        <SourceLocation DefaultValue="https://{domain}/taskpane.html"/>
        <RequestedHeight>450</RequestedHeight>
      </DesktopSettings>
    </Form>
  </FormSettings>

  <!-- Permissions: ReadWriteItem is sufficient for current-item operations -->
  <Permissions>ReadWriteItem</Permissions>

  <!-- Activation Rules -->
  <Rule xsi:type="RuleCollection" Mode="Or">
    <Rule xsi:type="ItemIs" ItemType="Message" FormType="Read"/>
    <Rule xsi:type="ItemIs" ItemType="Message" FormType="Edit"/>
  </Rule>

  <DisableEntityHighlighting/>

  <!-- Version Overrides for Modern Clients -->
  <VersionOverrides xmlns="http://schemas.microsoft.com/office/mailappversionoverrides" xsi:type="VersionOverridesV1_0">
    <VersionOverrides xmlns="http://schemas.microsoft.com/office/mailappversionoverrides/1.1" xsi:type="VersionOverridesV1_1">

      <Requirements>
        <bt:Sets DefaultMinVersion="1.5">
          <bt:Set Name="Mailbox"/>
        </bt:Sets>
      </Requirements>

      <Hosts>
        <Host xsi:type="MailHost">
          <DesktopFormFactor>

            <!-- Function File (for event-based activation / Smart Alerts) -->
            <FunctionFile resid="residFunctionFile"/>

            <!-- Message Read: task pane button in ribbon -->
            <ExtensionPoint xsi:type="MessageReadCommandSurface">
              <OfficeTab id="TabDefault">
                <Group id="quillGroup">
                  <Label resid="residGroupLabel"/>

                  <Control xsi:type="Button" id="quillTaskPaneButton">
                    <Label resid="residButtonLabel"/>
                    <Supertip>
                      <Title resid="residButtonLabel"/>
                      <Description resid="residButtonDesc"/>
                    </Supertip>
                    <Icon>
                      <bt:Image size="16" resid="residIcon16"/>
                      <bt:Image size="32" resid="residIcon32"/>
                      <bt:Image size="80" resid="residIcon80"/>
                    </Icon>
                    <Action xsi:type="ShowTaskpane">
                      <SourceLocation resid="residTaskPaneUrl"/>
                    </Action>
                  </Control>
                </Group>
              </OfficeTab>
            </ExtensionPoint>

            <!-- Message Compose: task pane button in ribbon -->
            <ExtensionPoint xsi:type="MessageComposeCommandSurface">
              <OfficeTab id="TabDefault">
                <Group id="quillComposeGroup">
                  <Label resid="residGroupLabel"/>

                  <Control xsi:type="Button" id="quillComposeButton">
                    <Label resid="residButtonLabel"/>
                    <Supertip>
                      <Title resid="residButtonLabel"/>
                      <Description resid="residButtonDesc"/>
                    </Supertip>
                    <Icon>
                      <bt:Image size="16" resid="residIcon16"/>
                      <bt:Image size="32" resid="residIcon32"/>
                      <bt:Image size="80" resid="residIcon80"/>
                    </Icon>
                    <Action xsi:type="ShowTaskpane">
                      <SourceLocation resid="residTaskPaneUrl"/>
                    </Action>
                  </Control>
                </Group>
              </OfficeTab>
            </ExtensionPoint>

          </DesktopFormFactor>
        </Host>
      </Hosts>

      <!-- Resources -->
      <Resources>
        <bt:Images>
          <bt:Image id="residIcon16" DefaultValue="https://{domain}/assets/icon-16.png"/>
          <bt:Image id="residIcon32" DefaultValue="https://{domain}/assets/icon-32.png"/>
          <bt:Image id="residIcon80" DefaultValue="https://{domain}/assets/icon-80.png"/>
        </bt:Images>
        <bt:Urls>
          <bt:Url id="residFunctionFile" DefaultValue="https://{domain}/commands.html"/>
          <bt:Url id="residTaskPaneUrl" DefaultValue="https://{domain}/taskpane.html"/>
        </bt:Urls>
        <bt:ShortStrings>
          <bt:String id="residGroupLabel" DefaultValue="Quill AI"/>
          <bt:String id="residButtonLabel" DefaultValue="Open Quill"/>
        </bt:ShortStrings>
        <bt:LongStrings>
          <bt:String id="residButtonDesc" DefaultValue="Open the Quill AI assistant to summarize emails, draft replies, extract data, and translate."/>
        </bt:LongStrings>
      </Resources>

      <!-- No WebApplicationInfo: this add-in does not use Graph API or Entra SSO -->

    </VersionOverrides>
  </VersionOverrides>

</OfficeApp>
